<nav class="navbar navbar-expand-lg">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand" do %>
      <i class="fas fa-cat"></i> Crazy4Cats <i class="fas fa-cat"></i>
    <% end %>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <div class="d-flex align-items-center">
              <% if current_user.profile_image.attached? %>
                <%= image_tag current_user.profile_image, class: "rounded-image" %>
              <% else %>
                <%= image_tag "default_profile_image.png", class: "rounded-image" %>
              <% end %>
              <span class="nav-link"><%= current_user.name %></span>
            </div>
          </li>
          <li class="nav-item">
            <%= link_to "Editar perfil", edit_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to "Salir", destroy_user_session_path, method: :delete, class: "nav-link" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Crea tu cuenta", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Ingresa", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
